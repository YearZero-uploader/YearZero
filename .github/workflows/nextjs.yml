name: Deploy Next.js Frontend

on:
  push:
    branches: ["main"] # Runs every time you push to main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: 'npm'
          cache-dependency-path: './frontend/yearzero-frontend/package-lock.json'

      - name: Install dependencies
        run: npm install
        working-directory: ./frontend/yearzero-frontend

      - name: Build Next.js
        run: npm run build
        working-directory: ./frontend/yearzero-frontend

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Next.js static export creates an 'out' folder
          path: ./frontend/yearzero-frontend/out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
